//use client is important bacause everything in the nextjs is a server thing thus if we want to get some data from the user(client) we will have to convert to it a client component.
"use client";


export default function signupuser(){
    return(
        <div>signup</div>
    )
}